{
  "name": "deep-merge-challenge",
  "version": "0.1.0",
  "private": true,
  "description": "Type-safe deep-merge utility coding challenge",
  "type": "module",
  "scripts": {
    "test": "vitest run --reporter=verbose",
    "test:tag": "vitest run --reporter=verbose -t",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "typescript": "5.4.5",
    "vitest": "1.6.0"
  }
}
