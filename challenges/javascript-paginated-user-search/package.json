{
  "name": "paginated-user-search-challenge",
  "version": "0.1.0",
  "private": true,
  "description": "Debounced paginated user-search controller coding challenge",
  "type": "module",
  "scripts": {
    "test": "vitest run --reporter=verbose",
    "test:tag": "vitest run --reporter=verbose -t"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "1.6.0",
    "vitest": "1.6.0"
  }
}
